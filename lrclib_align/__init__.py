"""lrclib_align — LRCLIB-anchored chunked alignment.

Phase 1 surface:
    - search.interactive_search()  → Genius search + top-10 picker
    - lrclib.find_match()          → LRCLIB /api/get + /api/search gate
    - run.main()                   → CLI orchestrator
"""
